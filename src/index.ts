import { LanguageFilter } from "./LanguageFilter";

import { analyzeText, getSafeText } from "./Logic";
import { regexTemplate } from "./regexTemplates";

export { LanguageFilter, analyzeText, getSafeText, regexTemplate };
