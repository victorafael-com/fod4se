import { LanguageFilter } from "./LanguageFilter";

import { analyzeText, getSafeText, getTextBlocks } from "./Logic";
import { regexTemplate } from "./regexTemplates";

export {
  LanguageFilter,
  analyzeText,
  getSafeText,
  getTextBlocks,
  regexTemplate,
};
